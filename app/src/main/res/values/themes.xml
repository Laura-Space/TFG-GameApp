<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Game_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales. -->
        <item name="colorPrimary">@color/principal</item>
        <item name="colorPrimaryVariant">@color/variantePrincipal</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colores secundarios. -->
        <item name="colorSecondary">@color/secundario</item>
        <item name="colorSecondaryVariant">@color/varianteSecundario</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status Bar. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Menú lateral. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground"></item>
    </style>
</resources>